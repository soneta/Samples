using Soneta.Types;

namespace Soneta.Example.AsyncWorker
{
    public static class Dummy
    {
        // Reference to Soneta.Types to force load page infos
        // ReSharper disable once UnusedMember.Local
        private static readonly Date Today = Date.Today;
    }
}